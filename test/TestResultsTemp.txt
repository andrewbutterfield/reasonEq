addKnownVarSet lv (sngl glw) newVarTable
=
{  }
{  }
{ lv$ ≜ {lw$} }

addKnownVarSet lu (sngl glv) newVarTable
=
{  }
{  }
{ lu$ ≜ {lv$} }

addKnownVarSet lu (sngl glv) (fromJust (addKnownVarSet lv (sngl glw) newVarTable))
=


dtList (fromJust (addKnownVarSet lu (sngl glv)
          (fromJust (addKnownVarSet lv (sngl glw) newVarTable))))
=


--

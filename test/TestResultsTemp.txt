vsMatch [vtS_Design] emptyBinding S.empty S.empty (vswrap [x,y,z]) (vswrap [vu] `S.union` lswrap [lS `less` ([u],[])])

vsMatch [vtS_Design] emptyBinding S.empty S.empty
    (vswrap [x,y,z])
    (vswrap [vu] `S.union` lswrap [lS `less` ([u],[])])
=
*MatchScenarios> vsMatch [vtS_Design] emptyBinding S.empty S.empty (vswrap [x,y,z]) (vswrap [vu] `S.union` lswrap [lS `less` ([u],[])])

vsKM.vsC = {y,z}

vsKM.gvP = S\u

*** Exception: user error (vsMatch: some pattern var-set is not in candidate.

vsKx = [x,y,z]

vsCx = [y,z]
)

--

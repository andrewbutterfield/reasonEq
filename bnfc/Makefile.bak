## File generated by the BNF Converter (bnfc 2.9.7).

# Makefile for building the parser and test program.

GHC        = ghc
HAPPY      = happy
HAPPY_OPTS = --array --info --ghc --coerce
ALEX       = alex
ALEX_OPTS  = --ghc

# List of goals not corresponding to file names.

.PHONY : all deploy clean distclean

# Default goal.

all : REQ/Test

deploy : cp REQ ../src/REQ

# Rules for building the parser.

REQ/Abs.hs REQ/Lex.x REQ/Par.y REQ/Print.hs REQ/Test.hs : REQ.lbnf
	bnfc --haskell -d REQ.lbnf

%.hs : %.y
	${HAPPY} ${HAPPY_OPTS} $<

%.hs : %.x
	${ALEX} ${ALEX_OPTS} $<

REQ/Test : REQ/Abs.hs REQ/Lex.hs REQ/Par.hs REQ/Print.hs REQ/Test.hs
	${GHC} ${GHC_OPTS} $@

# Rules for cleaning generated files.

clean :
	-rm -f REQ/*.hi REQ/*.o REQ/*.log REQ/*.aux REQ/*.dvi

distclean : clean
	-rm -f REQ/Abs.hs REQ/Abs.hs.bak REQ/ComposOp.hs REQ/ComposOp.hs.bak REQ/Doc.txt REQ/Doc.txt.bak REQ/ErrM.hs REQ/ErrM.hs.bak REQ/Layout.hs REQ/Layout.hs.bak REQ/Lex.x REQ/Lex.x.bak REQ/Par.y REQ/Par.y.bak REQ/Print.hs REQ/Print.hs.bak REQ/Skel.hs REQ/Skel.hs.bak REQ/Test.hs REQ/Test.hs.bak REQ/XML.hs REQ/XML.hs.bak REQ/AST.agda REQ/AST.agda.bak REQ/Parser.agda REQ/Parser.agda.bak REQ/IOLib.agda REQ/IOLib.agda.bak REQ/Main.agda REQ/Main.agda.bak REQ/REQ.dtd REQ/REQ.dtd.bak REQ/Test REQ/Lex.hs REQ/Par.hs REQ/Par.info REQ/ParData.hs Makefile
	-rmdir -p REQ/

# EOF

The Language REQ
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of REQ==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.




DynVar literals are recognized by the regular expression
`````('_' | letter) ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in REQ are the following:
  | ``BndL`` | ``BndS`` | ``Conjecture`` | ``DclASet``
  | ``DclDLVar`` | ``DclVar`` | ``Exp`` | ``ExprVars``
  | ``False`` | ``Law`` | ``NA`` | ``NS``
  | ``Obs`` | ``ObsVars`` | ``Prd`` | ``PredVars``
  | ``SB`` | ``SC`` | ``StaticVars`` | ``Sub``
  | ``SubL`` | ``SubV`` | ``Theory`` | ``True``
  | ``VSC`` | ``assumed`` | ``axiom`` | ``covby``
  | ``dcovby`` | ``disj`` | ``fresh`` | ``lst``
  | ``nil`` | ``none`` | ``proven`` | ``std``
  | ``tbot`` | ``ttop`` | ``var`` |

The symbols used in REQ are the following:
  | . | === | ==> | \/
  | /\ | ~ | == | !=
  | < | <= | > | >=
  | ++ | : | + | -
  | * | / | % | (
  | ) | [ | ] | [.
  | .] | | | , | ->
  | (: | :) | ; |

===Comments===
There are no single-line comments in the grammar. There are no multiple-line comments in the grammar.

==The syntactic structure of REQ==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Thry// | -> | ``Theory`` //DynVar// ``.`` //[DynVar]// ``.`` //[Item]//
  | //[DynVar]// | -> | **eps**
  |  |  **|**  | //DynVar// //[DynVar]//
  | //Item// | -> | ``ObsVars`` //[DynVar]// ``.``
  |  |  **|**  | ``ExprVars`` //[DynVar]// ``.``
  |  |  **|**  | ``PredVars`` //[DynVar]// ``.``
  |  |  **|**  | ``StaticVars`` //[DynVar]// ``.``
  |  |  **|**  | ``DclVar`` //VClass// //DynVar// ``.`` //VarRole// ``.``
  |  |  **|**  | ``DclDLVar`` //VClass// //DynVar// ``.`` //[DynVar]// ``.``
  |  |  **|**  | ``DclASet`` //VClass// //DynVar// ``.``
  |  |  **|**  | ``Law`` //LawType// //DynVar// ``.`` //Trm// ``.`` //SCond//
  |  |  **|**  | ``Conjecture`` //DynVar// ``.`` //Trm// ``.`` //SCond//
  | //VarRole// | -> | ``var`` //SBBL// //Typ//
  | //VClass// | -> | ``Obs``
  |  |  **|**  | ``Exp``
  |  |  **|**  | ``Prd``
  | //SBBL// | -> | ``NA``
  |  |  **|**  | ``SB``
  |  |  **|**  | ``NS``
  | //LawType// | -> | ``axiom``
  |  |  **|**  | ``proven``
  |  |  **|**  | ``assumed``
  | //[Item]// | -> | **eps**
  |  |  **|**  | //Item// //[Item]//
  | //Trm// | -> | //Trm// ``===`` //Trm1//
  |  |  **|**  | //Trm1//
  | //Trm1// | -> | //Trm2// ``==>`` //Trm1//
  |  |  **|**  | //Trm2//
  | //Trm2// | -> | //Trm2// ``\/`` //Trm3//
  |  |  **|**  | //Trm3//
  | //Trm3// | -> | //Trm3// ``/\`` //Trm4//
  |  |  **|**  | ``~`` //Trm4//
  |  |  **|**  | //Trm4//
  | //Trm4// | -> | //Trm5// ``==`` //Trm5//
  |  |  **|**  | //Trm5// ``!=`` //Trm5//
  |  |  **|**  | //Trm5// ``<`` //Trm5//
  |  |  **|**  | //Trm5// ``<=`` //Trm5//
  |  |  **|**  | //Trm5// ``>`` //Trm5//
  |  |  **|**  | //Trm5// ``>=`` //Trm5//
  |  |  **|**  | //Trm5//
  | //Trm5// | -> | //Trm6// ``++`` //Trm5//
  |  |  **|**  | //Trm6// ``:`` //Trm5//
  |  |  **|**  | //Trm6//
  | //Trm6// | -> | //Trm6// ``+`` //Trm7//
  |  |  **|**  | //Trm6// ``-`` //Trm7//
  |  |  **|**  | //Trm7//
  | //Trm7// | -> | //Trm7// ``*`` //Trm8//
  |  |  **|**  | //Trm7// ``/`` //Trm8//
  |  |  **|**  | //Trm7// ``%`` //Trm8//
  |  |  **|**  | ``-`` //Trm8//
  |  |  **|**  | //Trm8//
  | //Trm8// | -> | //Integer//
  |  |  **|**  | //DynVar//
  |  |  **|**  | ``True``
  |  |  **|**  | ``False``
  |  |  **|**  | ``nil``
  |  |  **|**  | //DynVar// ``(`` //[Trm]// ``)``
  |  |  **|**  | ``BndS`` //DynVar// ``[`` //[DynVar]// ``]`` //Trm//
  |  |  **|**  | ``BndL`` //DynVar// ``[`` //[DynVar]// ``]`` //Trm//
  |  |  **|**  | ``SubV`` //Trm// ``[.`` //[Trm]// ``/`` //[DynVar]// ``.]``
  |  |  **|**  | ``SubL`` //Trm// ``[.`` //[DynVar]// ``/`` //[DynVar]// ``.]``
  |  |  **|**  | ``Sub`` //Trm// ``[.`` //[Trm]// ``/`` //[DynVar]// ``|`` //[DynVar]// ``/`` //[DynVar]// ``.]``
  |  |  **|**  | ``(`` //Trm// ``)``
  | //[Trm]// | -> | **eps**
  |  |  **|**  | //Trm//
  |  |  **|**  | //Trm// ``,`` //[Trm]//
  | //Typ// | -> | //Typ1// ``->`` //Typ//
  |  |  **|**  | //Typ1//
  | //Typ1// | -> | //DynVar// ``(:`` //[Typ2]// ``:)``
  |  |  **|**  | //Typ2//
  | //Typ2// | -> | //DynVar// //[Typ3]//
  |  |  **|**  | //Typ3//
  | //Typ3// | -> | ``ttop``
  |  |  **|**  | //DynVar//
  |  |  **|**  | ``tbot``
  |  |  **|**  | ``(`` //Typ// ``)``
  | //[Typ2]// | -> | **eps**
  |  |  **|**  | //Typ2//
  |  |  **|**  | //Typ2// ``|`` //[Typ2]//
  | //[Typ3]// | -> | **eps**
  |  |  **|**  | //Typ3// //[Typ3]//
  | //GVar// | -> | ``std`` //DynVar//
  |  |  **|**  | ``lst`` //DynVar//
  | //[GVar]// | -> | **eps**
  |  |  **|**  | //GVar//
  |  |  **|**  | //GVar// ``,`` //[GVar]//
  | //VrSet// | -> | //[GVar]//
  | //VSCond// | -> | //GVar// ``disj`` //VrSet//
  |  |  **|**  | //GVar// ``covby`` //VrSet//
  |  |  **|**  | //GVar// ``dcovby`` //VrSet//
  | //[VSCond]// | -> | **eps**
  |  |  **|**  | //VSCond//
  |  |  **|**  | //VSCond// ``;`` //[VSCond]//
  | //SCond// | -> | ``SC`` //[VSCond]// ``.`` //VrSet// ``.``
  |  |  **|**  | ``VSC`` //[VSCond]// ``.``
  |  |  **|**  | ``fresh`` //VrSet// ``.``
  |  |  **|**  | ``none``



%% File generated by the BNF Converter (bnfc 2.9.7).
